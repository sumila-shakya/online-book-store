info:
  name: refresh_token
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/auth/refresh
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
