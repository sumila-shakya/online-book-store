info:
  name: browse_books
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/books?q=Emily
  params:
    - name: q
      value: Emily
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
